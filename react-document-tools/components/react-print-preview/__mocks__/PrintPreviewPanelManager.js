// We mock popup panels
import React from 'react';

const PrintPreviewPanelManager = ({ pages }) => (
  <>
    PrintPreviewPanelManager Mock
    <div>{pages}</div>
  </>
);

export default PrintPreviewPanelManager;
